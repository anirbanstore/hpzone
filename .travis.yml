language: node_js

node_js:
 - lts/*

addons:
  chrome: stable

cache:
  paths:
    - node_modules/

before_script:
 - npm install -g firebase-tools

script:
  - ng test --watch=false
  - ng build --aot --prod

branches:
 only:
  - master

deploy:
  skip_cleanup: true
  provider: script
  script: firebase deploy --token $FIREBASE_TOKEN
  on:
    branch: master
